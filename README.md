# KillerBayer FBO

Статический прототип маркетплейса для Якутии с демо-админ-панелью.

## Запуск локально

```bash
python -m http.server 8000
```

Откройте:
- `http://localhost:8000/` — основной сайт.
- `http://localhost:8000/admin.html` — админ-панель (сохранение в localStorage).
